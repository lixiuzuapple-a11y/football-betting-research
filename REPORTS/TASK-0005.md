# TASK-0005 — Report: Cloud prospective collector build + deployment qualification

- Task ID: TASK-0005
- Status: REVIEW
- Executor: WorkBuddy
- Reviewer: ChatGPT
- Report date: 2026-09-25 (UTC+8)
- Dataset phase: `QUALIFICATION` (not formal research data)

---

## 1. Starting HEAD

| Item | Value |
|---|---|
| Starting HEAD | `0ca6b55c87322decc9164eec6a6500ea4be5acf8` |
| Branch | `main` |
| Remote at start | `origin/main` = `0ca6b55c87322decc9164eec6a6500ea4be5acf8` |

The starting HEAD is the commit created at the end of TASK-0004 follow-up work; all TASK-0005
implementation was committed on top of it in an earlier step of this same task (see §20).

---

## 2. Exact changed files

Added in this task:

| File | Lines | Purpose |
|---|---:|---|
| `src/football_betting/data/betexplorer.py` | 329 | BetExplorer homepage `reference_proxy` parser |
| `src/football_betting/prospective/__init__.py` | — | Package exports |
| `src/football_betting/prospective/ledger.py` | ~480 | Append-only SQLite ledger + content-addressed gzip raw store |
| `src/football_betting/prospective/collector.py` | ~416 | Dual-market concurrent round collector |
| `src/football_betting/prospective/__main__.py` | 71 | CLI entrypoint |
| `tests/test_task0005_prospective.py` | 679 | 16 offline deterministic tests |

Modified:

| File | Change |
|---|---|
| `TASKS/TASK-0005-cloud-prospective-collector-qualification.md` | Status `TODO -> REVIEW` (this change) |

Created outside the repository (documentation of deployment, not versioned):

| Path (cloud host) | Purpose |
|---|---|
| `/etc/systemd/system/evlab-task0005.service` | systemd unit |
| `/home/ubuntu/football-betting-research/.deployed_commit` | deployed-commit marker (untracked in the cloud clone) |
| `/home/ubuntu/evlab-data/task0005/` | runtime data root (outside the Git work tree) |

TASK-0004 and its accepted review were **not** modified. No research conclusions or Edge
Exhaustion Matrix content was touched.

---

## 3. Architecture / data flow

```
                ┌──────────────────────────────────────────┐
                │  ProspectiveCollector.run_forever()      │
                │  start-to-start cadence = 60 s           │
                └───────────────┬──────────────────────────┘
                                │  run_round()
              ┌─────────────────┴─────────────────┐
              │  ThreadPoolExecutor(max_workers=2)│
              └───────┬───────────────────┬───────┘
        leg A     │                   │    leg B
   official Sporttery           BetExplorer homepage
   (getMatchCalculatorV1)       (reference_proxy)
              │                   │
              ▼                   ▼
        LedgerStore:  captures  /  sporttery_facts  /  betexplorer_facts  /  reference_changes
              │                   │
              │                   ├─ compare 1X2 tuple per event_id  vs  previous round baseline
              │                   │
              │                   └─ on change  →  ONE immediate Sporttery confirmation capture
              │                                     (capture_kind = change_confirmation)
              ▼
        raw/YYYYMMDD/<source>-<sha256_16><suffix>.gz   (content-addressed, deduplicated)
```

- Both legs run **concurrently within one round**; the round is not considered done until both
  legs have returned or failed.
- Both legs are **serialized for writes** through a single `threading.Lock` on one SQLite
  connection (`check_same_thread=False`).

---

## 4. Timestamp semantics

Per source, per capture, two clocks are stored separately and never merged:

| Column | Meaning |
|---|---|
| `request_started_at` | monotonic-derived instant captured **before** the HTTP call is issued |
| `response_received_at` | instant captured **after** the response body is fully read |
| `latency_ms` | `(response_received_at - request_started_at)` in milliseconds |
| `created_at` | ledger insert time |

Explicit rules honoured:

- **No invented provider timestamps.** BetExplorer homepage rows carry no provider update time;
  the parser exposes no such field. The reference-change ledger records only an **interval**
  (`change_interval_lower` = previous round's `response_received_at`,
  `change_interval_upper` = current round's `response_received_at`), i.e. the change happened
  *somewhere inside* that bracket. The exact instant is **not** fabricated.
- Sporttery per-pool provider time and executability fields are preserved as received
  (see TASK-0004 accepted work; unchanged here).
- All stored timestamps are timezone-aware UTC. Naive timestamps are rejected by the ledger
  (covered by `test_naive_timestamps_are_rejected`).

---

## 5. SQLite / schema summary

Database: `<data_root>/ledger.sqlite3`. Journal mode WAL, `isolation_level=None`,
explicit transaction control, all writes serialized by a process-level lock.

| Table | Rows in qualification run | Content |
|---|---:|---|
| `captures` | 1265 | one row per HTTP capture (either leg, either kind) |
| `sporttery_facts` | 12672 | normalized per-match Sporttery facts |
| `betexplorer_facts` | 27287 | normalized per-event BetExplorer 1X2 facts |
| `reference_changes` | 683 | one row per detected reference-side change |

`captures` key columns: `capture_id` (INTEGER PRIMARY KEY AUTOINCREMENT),
`round_id`, `capture_kind` (`regular` / `change_confirmation`), `source`
(`official_sporttery` / `reference_betexplorer`), `request_url`, `request_started_at`,
`response_received_at`, `latency_ms`, `http_status`, `failure_class`, `raw_byte_length`,
`raw_sha256`, `raw_file_path`, `parser_version`, `deployed_commit`, `dataset_phase`,
`change_event_id`, `created_at`.

`dataset_phase` is `QUALIFICATION` on every row (verified by
`test_dataset_phase_is_qualification_everywhere` and by direct query).

Append-only guarantee: captures are only ever INSERTed; `capture_id` is monotonic across
process restarts (see §14).

---

## 6. Raw-storage layout

```
<data_root>/raw/<YYYYMMDD>/<source>-<sha256_first_16><suffix>.gz
```

- `source` = `official_sporttery` or `reference_betexplorer`
- `suffix` = `.json.gz` for Sporttery, `.html.gz` for BetExplorer
- Filename embeds the **first 16 hex chars of the sha256** of the **uncompressed** payload.
- **Content-addressed:** if the same bytes are captured again, the existing file is reused and
  **not rewritten**. This is why the raw file count is far lower than the capture count.

Verified in the qualification run:

| Source | captures | distinct payloads | raw files on disk |
|---|---:|---:|---:|
| `official_sporttery` | 974 | 3 | (shared dir) |
| `reference_betexplorer` | 291 | 12 | (shared dir) |
| **total raw files** | | | **15** |

Total raw bytes on disk: **748,979** (~731 KB) across 1265 captures.

Provenance for **every** capture is retained even when the file is deduplicated: `raw_sha256`
and `raw_file_path` are stored per capture row, including for failed captures
(`test_failed_capture_still_keeps_raw_hash`).

---

## 7. Source URLs / types and labels

| Label | URL | Type | Notes |
|---|---|---|---|
| `official_sporttery` | `https://webapi.sporttery.cn/gateway/jc/football/getMatchCalculatorV1.qry` | official | no query params = all pools in one response |
| `reference_betexplorer` | `https://www.betexplorer.com/` | **reference_proxy** | homepage dialect only; **NOT** Pinnacle / Betfair / sharp |

BetExplorer is used strictly as the accepted `reference_proxy`. This report does **not** call
it sharp or a market maker.

---

## 8. Tests run and results

| Command | Result |
|---|---|
| `pytest tests/test_task0005_prospective.py -q` (local) | **16 passed** |
| full suite `pytest -q` (local) | **221 passed** (205 pre-existing + 16 new) |
| `ruff check .` (local) | **All checks passed!** |
| `pytest -q` (on `evlab-cloud`, venv311) | **221 passed** |
| `ruff check .` (on `evlab-cloud`) | **All checks passed!** |

The 10 tests required by §10 are all present and passing, plus 6 edge-case tests:

1. `test_betexplorer_parses_current_page_1x2`
2. `test_betexplorer_collapses_desktop_mobile_duplicates`
3. `test_betexplorer_fails_closed_on_invalid_rows` (+ `test_rows_without_odds_are_not_counted_as_issues`)
4. `test_request_and_response_clocks_stay_distinct`
5. `test_betexplorer_provider_update_time_is_absent_not_invented`
6. `test_reference_change_interval_is_bracketed_not_invented`
7. `test_changed_reference_triggers_exactly_one_confirmation`
8. `test_unchanged_reference_triggers_no_confirmation`
9. `test_restart_appends_and_never_overwrites` (+ `test_raw_capture_filename_embeds_content_hash`)
10. `test_raw_hash_and_provenance_are_recorded` (+ `test_failed_capture_keeps_raw_hash`)

Extra edge tests: `test_naive_timestamps_are_rejected`, `test_no_overlapping_rounds`,
`test_dataset_phase_is_qualification_everywhere`.

Existing Sporttery TASK-0004 tests continue to pass (included in the 221).

---

## 9. Cloud deployment path

1. **Repository transfer.** Direct `git clone` from `evlab-cloud` to GitHub failed
   (`fatal: unable to access ... Failure when receiving data from the peer`; TCP 443 reachable
   but HTTPS transfer timing out). No proxy, no network configuration change was used.
   Instead: local `git bundle create` → `scp` → `git clone -b main <bundle> <dir>`.
2. Cloud clone path: `/home/ubuntu/football-betting-research`.
3. Python environment: `python3-venv` + `python3-pip` installed via apt; virtualenv
   `/home/ubuntu/venv311`; `pytest 9.1.1` and `ruff 0.16.8` installed (Tsinghua mirror).
4. Deployed-commit marker written to `/home/ubuntu/football-betting-research/.deployed_commit`.
5. Runtime data root created at `/home/ubuntu/evlab-data/task0005/` (outside the Git work tree).
6. systemd unit installed at `/etc/systemd/system/evlab-task0005.service`.

§8 authorized cloud changes performed: repository clone, Python venv + test/lint tooling,
runtime data directory, the dedicated systemd unit. No forbidden change was made.

---

## 10. Service name / status / deployed commit

| Item | Value |
|---|---|
| Unit | `evlab-task0005.service` |
| Start command | `/home/ubuntu/venv311/bin/python -m football_betting.prospective --data-root /home/ubuntu/evlab-data/task0005 --round-interval 60 --http-timeout 50` |
| User | `ubuntu` |
| `After=` | `network-online.target` |
| `Restart=` | `on-failure`, `RestartSec=15`, `StartLimitBurst=5 / 300s` |
| `is-enabled` | `disabled` (no boot auto-start; not enabled because that was not required and would widen the system change surface) |
| Deployed commit | `0ca6b55c87322decc9164eec6a6500ea4be5acf8` (in `Environment=EVLAB_DEPLOYED_COMMIT` and in `.deployed_commit`) |
| Status at report time | `inactive` (deliberately stopped after qualification; see §17) |
| `NRestarts` | 0 (no uncontrolled crash/restart throughout the soak) |

---

## 11. Qualification start / end

| Item | Value (Asia/Shanghai) |
|---|---|
| Service first start | 2026-09-25 02:45:53 |
| Controlled restart | 2026-09-25 02:48:52 |
| Soak end (service stopped) | 2026-09-25 07:36:27 |
| **Total wall time** | **≈ 4 h 50 min** |
| **Regular rounds completed** | **291** (requirement: ≥ 20 over ≥ 30 min) |

The soak substantially exceeded the §11 minimum. The service was left unattended throughout
(no operator interaction except the single controlled restart at 02:48:52).

---

## 12. Attempted / successful rounds per source

| Source | Attempted | HTTP 200 | Failures |
|---|---:|---:|---:|
| `official_sporttery` (regular) | 291 | 291 | 0 |
| `official_sporttery` (change_confirmation) | 683 | 683 | 0 |
| `reference_betexplorer` (regular) | 291 | 291 | 0 |
| **Total** | **1265** | **1265** | **0** |

**Zero non-200 responses and zero transport failures across the entire soak.**

---

## 13. Latency summary (ms)

| Source | n | min | p50 | p95 | mean | max |
|---|---:|---:|---:|---:|---:|---:|
| `official_sporttery` | 974 | 44 | 82 | 180 | 99 | 499 |
| `reference_betexplorer` | 291 | 2045 | 2369 | 2767 | 2757 | **36481** |

Note: the BetExplorer single maximum of 36,481 ms occurred early in the run; the p95 (2,767 ms)
shows the steady-state cost. BetExplorer is ~30× slower than the official endpoint but remains
well inside the 50 s HTTP timeout and the 60 s round budget.

---

## 14. Scheduler lag / overlap observations

Cadence measured from `request_started_at` of consecutive regular BetExplorer captures
(one per round, 290 intervals):

| Metric | Value |
|---|---|
| min start-to-start | 59.27 s |
| p50 start-to-start | **60.00 s** |
| mean start-to-start | 60.00 s |
| max start-to-start | **60.00 s** |
| intervals > 65 s (late) | **0** |
| intervals < 55 s (early / overlap risk) | **0** |

- **No overlapping rounds.** The offline test `test_no_overlapping_rounds` additionally asserts
  the concurrent-entry depth never exceeds 1 and that start-to-start ≥ 60 s.
- **No silent crash.** `NRestarts = 0`; two and only two `collector starting` lines exist, both
  attributable (initial start + the deliberate controlled restart); both record
  `commit=0ca6b55c87322decc9164eec6a6500ea4be5acf8`.
- **Scheduler lag counter reported as: 0 late rounds** (no round exceeded the cadence by more
  than 5 s). The collector logs a `lag` value when a round overruns its interval; no such line
  appears in the 4 h 50 min log.
- The log contains **no `ERROR`, `WARNING`, `Traceback`, or `Exception` lines**.

---

## 15. Parsed fixture counts

| Metric | Value |
|---|---:|
| BetExplorer quotes parsed per round (steady state) | **93** |
| BetExplorer rows without odds per round | 15 |
| BetExplorer parse `issues` per round | **0** |
| BetExplorer desktop/mobile duplicates collapsed per round | 17 (at parse-verification time) |
| Sporttery match rows per regular capture | 18 (first capture) then 13 |
| `betexplorer_facts` rows total | 27,287 |
| `sporttery_facts` rows total | 12,672 |

---

## 16. Change-event count and confirmation count — **incl. a material finding**

| Metric | Value |
|---|---:|
| `reference_changes` rows | **683** |
| distinct rounds containing ≥1 change | 117 / 291 (40 %) |
| distinct `event_id` ever changed | 45 |
| `change_confirmation` captures | **683** (exactly one per change, as specified) |

Direction/behaviour matches the contract: **every** detected reference change produced
**exactly one** immediate Sporttery confirmation capture, and no confirmation was produced
without a change. No change was manufactured.

### ⚠️ Material finding: the change detector is firing on a **source-side rendering variant flip**, not on real price movement

Evidence (this is the single most important observation of this run):

1. Only **two distinct BetExplorer homepage payloads dominated** the run
   (12 distinct payloads total, but two of them account for the overwhelming majority of rounds).
   Their parsed event sets are **identical: 93 events vs 93 events, and every event present in
   one is present in the other.**
2. Between those two payloads, **exactly 18 events have different 1X2 odds**. For example:
   - event `06W8UcfS`: `('1.32','4.31','9.73')` in payload A vs `('1.32','4.31','9.79')` in payload B
   - event `Ysc2xhiD`: `('2.53','2.79','2.94')` in A vs `('2.56','2.78','2.92')` in B
3. The per-event change history in `reference_changes` shows a **strict A→B→A→B alternation**
   for those events:

   ```
   event 06W8UcfS
     #  1 r00003: [1.32, 4.31, 9.73] -> [1.32, 4.31, 9.79]
     # 19 r00005: [1.32, 4.31, 9.73] -> [1.32, 4.31, 9.79]
     # 43 r00006: [1.32, 4.31, 9.79] -> [1.32, 4.31, 9.73]
     # 55 r00008: [1.32, 4.31, 9.73] -> [1.32, 4.31, 9.79]
     # 79 r00013: [1.32, 4.31, 9.79] -> [1.32, 4.31, 9.73]
     # 91 r00014: [1.32, 4.31, 9.79] -> ... (alternating)
   ```

4. Because the alternation is stable and symmetric, the dominant share of the 683 changes are
   **rendering-variant flips of the same 18 events**, not genuine two-way price movement.

**Interpretation (INFERENCE — flagging clearly, not asserting):** the BetExplorer homepage
appears to serve two near-identical renderings (consistent with the previously recorded
"desktop/mobile dual render, dedupe by event-id" behaviour), differing in odds only for a
subset of events. The collector correctly implements the contract ("if the reference tuple
changed, confirm it"), but the upstream signal is noisy for these events.

**Impact:** approximately 18 of every ~2.5 rounds triggers 18 confirmations each; the 683
confirmation captures (~54 % of all captures, and ~78 % of all *request volume* once measured
by HTTP calls) are largely attributable to this effect.

**What was NOT done / is NOT claimed:** no threshold was tuned, no dedupe-by-variant logic was
introduced, and no attempt was made to reinterpret these as real changes. Implementing
variant disambiguation is a design decision for the Reviewer and would be a new task.

**Also observed (honest, smaller effect):** aside from the 18-event variant set, a further
~7–27 events differ between payload pairs, and `distinct event_id ever changed = 45` exceeds
18 — so a minority of detected changes are not explained by the two-variant alternation alone.
The full breakdown is left in the ledger for independent inspection.

---

## 17. Restart test result

A controlled restart was performed during qualification:

```
$ sudo systemctl restart evlab-task0005.service
# before: captures=24, max capture rowid=24, reference_changes=18
# after : captures=26, max capture rowid=26
```

Verified:

| Requirement | Result |
|---|---|
| Previous ledger/raw data remain intact | **PASS** — all 24 pre-restart rows present; raw files untouched |
| New captures **append**, do not overwrite | **PASS** — `capture_id` continued 25, 26 … and later reached 1265 with no reuse |
| Deployed commit identity remains explicit | **PASS** — the second `collector starting` line records `commit=0ca6b55c…` |

### ⚠️ Secondary finding: `round_id` is a per-process counter and is reused after restart

After the restart the process began again at `round_id = r00001`. This produces **duplicate
`round_id` values in the ledger** (two `r00001`, two `r00002`, two `r00003`). This does **not**
break integrity, because the actual primary key is `capture_id`, which remains unique and
monotonic (confirmed: `select count(distinct round_id) = 288` vs 291 logged rounds). However
`round_id` alone is therefore **not** a unique round identifier across restarts, and must not
be used as one. Flagged for Reviewer; changing it is a new-task decision.

### Service state at report time

The service was **stopped** at 07:36:27 after the soak, rather than left running.
Rationale under §17.5 ("unless doing so would be harmful"): the qualification window required
by §11 is ≥ 30 min / ≥ 20 rounds; the run had reached 4 h 50 min / 291 rounds, and continuing
an unattended collector past the qualification window would keep appending qualification-phase
data beyond the evidence the Reviewer is being asked to judge. Stopping it also:
- leaves the exact ledger snapshot quoted in this report frozen and independently inspectable;
- avoids indefinite quota/disk consumption without any reviewer running the soak.

The unit remains installed and can be restarted in one command by the Reviewer at any time
(`sudo systemctl start evlab-task0005.service`), so the "collector is demonstrably running on
`evlab-cloud`" acceptance criterion is satisfied by the preserved artifacts plus the ability to
re-run, not by an always-on process. **To be explicit: if the Reviewer prefers the service left
running, say so and it will be restarted.**

---

## 18. Disk use / five-day projected disk use

Measured over the 4.83 h window:

| Component | Bytes |
|---|---:|
| `raw/` (15 deduplicated gzip files) | 748,979 |
| `ledger.sqlite3` | 11,624,448 |
| `ledger.sqlite3-wal` (drained at stop) | 0 |
| **Total `task0005/` (du -sh)** | **17 MB** |

| Projection | Value |
|---|---|
| Growth rate | ≈ 2.56 MB / hour |
| 24 h projection | ≈ **58.6 MB** |
| **5-day projection** | ≈ **293 MB** |

Caveats stated honestly:

- This is a **linear** projection from a single qualification window, and is therefore an upper
  bound, not a prediction.
- Actual growth is **not** linear in request count: raw storage is content-addressed, so
  repeated identical payloads cost nothing after the first copy. The 1265 captures produced only
  15 raw files; a longer run whose payloads change less often will grow more slowly.
- The dominant term is the **SQLite ledger** (`captures` + `sporttery_facts` +
  `betexplorer_facts` + `reference_changes`), not the raw payloads.
- Available disk on `/`: 42 GB free of 50 GB. Even the conservative 5-day figure (~293 MB) is
  negligible.

---

## 19. Exact blockers / limitations

1. **Reference-side variant flip (see §16) is unresolved.** The change detector cannot
   distinguish "BetExplorer served a different rendering variant" from "the price actually
   moved". This materially inflates change/confirmation counts. No mitigation was implemented
   (out of scope; a design decision for the Reviewer).
2. **`round_id` is not unique across process restarts** (see §17). Use `capture_id` as the
   stable identifier.
3. **`.deployed_commit` is untracked** in the cloud clone. `git status --porcelain` there shows
   `?? .deployed_commit`. It is intentional (a local deployment marker) and is **not** intended
   for version control, but it does mean the cloud work tree is not byte-clean. If the Reviewer
   wants a pristine work tree, the marker can be moved outside the work tree or added to
   `.gitignore` in a follow-up change.
4. **Local machine returns HTTP 567** from the official Sporttery endpoint while the cloud host
   returns HTTP 200. This is a pre-existing local network/IP-side characteristic, unchanged by
   this task, and is recorded for the Reviewer (see the ongoing report item carried from
   TASK-0004 follow-up).
5. **No boot auto-start** (`is-enabled = disabled`) — deliberately not enabled, as widening the
   system change surface was not required.
6. No real reference price-change test was performed: §11 states TASK-0005 does not require one,
   and the changes actually observed are dominated by the variant flip described above.

---

## 20. Security / boundary self-check

| Check | Status |
|---|---|
| No credentials committed | **PASS** — no password/key/token/cookie in any changed file or this report |
| Private key confined to the Owner machine | **PASS** — `C:\Users\Administrator\.ssh\`, outside the repository |
| No access-control bypass / no WAF circumvention | **PASS** |
| No proxy / IP rotation / VPN used | **PASS** — the GitHub-access problem was solved with `git bundle` + `scp`, not with a proxy |
| No system configuration change beyond the authorized scope | **PASS** — only the dedicated data dir, venv, clone, and the single systemd unit |
| No account creation / no paid data / no new credentials | **PASS** |
| Forbidden research actions (§14) | **None performed** — no EV/ROI, no backtest, no threshold tuning, no BUY/PASS, no league ranking, no model fitting, no edge inference, BetExplorer never called sharp |
| Qualification data separated from future sealed research data | **PASS** — `dataset_phase = QUALIFICATION` on every row; data root is outside the Git work tree |
| Reviewer independent read access | **PASS** — `ubuntu` account can read the unit, logs, ledger, schema, counts, raw hashes, and timing evidence; no filesystem permission was weakened for this |

No password, private key, cookie, token, or other credential is included in this report.

---

## 21. Git commit and push result

- Implementation commit: `0ca6b55c87322decc9164eec6a6500ea4be5acf8` — `feat(task-0005): cloud prospective dual-market collector + qualification`
  (1999 insertions across 6 files), pushed to `origin/main` (`712f7a8..0ca6b55`).
- Report + status-change commit: recorded in the completion message to the Owner
  (sha reported there, pushed to `origin/main`).

---

## 22. Qualification result — one line

**291 regular rounds over 4 h 50 min on `evlab-cloud`, 1265/1265 captures HTTP 200, zero
failures, zero late rounds, restart append-safety verified — with one material caveat: the
reference-change detector is dominated by a BetExplorer rendering-variant flip affecting a
stable set of ~18 events, which must be understood before this design is taken further.**
