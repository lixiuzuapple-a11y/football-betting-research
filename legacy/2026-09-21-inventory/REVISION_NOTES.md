# REVISION_NOTES.md

> **修订轮次**：`REV-3`（2026-09-21）
> **依据**：Reviewer 第三轮审核 `REVIEW — NEEDS FINAL DOCUMENT CORRECTION (REV-3)`；随后正式验收时确认仅余一处纯文字计数 `4 → 3`，在 GitHub 入库副本中一并修正。
> **性质**：纯文档清理。**未重新扫描、未重跑实验、未碰旧数据、未修采集系统。**

**任务**：`INVENTORY-20260921-01` 修订（`REV-1`）
**依据**：Reviewer 6 项修订意见
**修订时点**：2026-09-21 11:2x
**修订前状态**：`REVIEW — NEEDS MINOR CORRECTION`

---

## 一、是否重新扫描过数据

**否。**

本次修订**未重新执行资产扫描**，全部复用第一次扫描的产出（`assets_raw.csv`，863 个文件）。

**唯一的例外（只读复核，非重扫）**：为验证 Reviewer 第 1 项（「在线盘点」）的要求，对 9 个活跃文件做了一次 **SHA256 只读复核**，用于确认「扫描期间是否真的发生了变化」。结果：**2 个已变**（`sporttery_odds.csv`、`forward_ledger.csv`），7 个未变。该复核**未写入任何被检文件**。

---

## 二、是否修改过任何旧资产

**否。**

- 未修改、未删除、未移动、未重命名**任何**既有文件
- 未修改任何历史数据文件
- 未修改任何脚本
- 未停止任何常驻采集进程
- 未删除任何重复文件

**本次全部写操作**：仅更新 `REPORTS/` 目录下的 4 份文档（+ 新建 1 份本文件）。

**独立验证**（修订后执行）：

```
find /workspace -maxdepth 1 -newermt "2026-09-21 10:20" -type f
→ （空，顶层零改动）
```

---

## 三、修改了哪些问题

### 第 1 项 · 「在线盘点」而非「静态归档快照」✅ 已改

| 位置 | 修改 |
|---|---|
| 主报告 新增 **§0.1** | 明确声明「**2026-09-21 时点的在线资产盘点。扫描期间部分活跃数据文件仍可能变化**」；列出 5 个常驻进程 PID；删去「静态快照」表述 |
| 主报告 新增 **§2.4** | 「LIVE 文件清单」表：9 个文件 × 扫描时/复核时 SHA256 对照，标注 `hash_time` |
| 主报告 §1.3 | 「863 为该时点的资产数」，不再用「准确静态快照数」 |
| 清单_数据资产.md 头部 | 新增 9 个 LIVE 文件说明表；这 9 行 SHA256 加 **`LIVE@10:22:`** 前缀 |
| 目录树.md 头部 | 加「在线盘点时点值」警示 + 2 个文件已变的实证 |
| 主报告 §6 数据流图 | 加 `L0/L1`、`L2`、`L3` 层级标注 |

**未暂停任何进程，未修改进程配置。**

### 第 2 项 · 重新定义资产层级 ✅ 已改

- 主报告 新增 **§1.4「资产层级定义（L0~L3）」**：给出四级定义 + 判据
- 主报告 §2.1 / §2.2：**所有**核心数据资产表新增 `Level` 列
- 清单_数据资产.md：**374 行**全部新增 `Level` 列
- **无法判断的标 `UNKNOWN`**：`hafu_hist.csv` 的 Level 判定标为「`L2` *（`INFERENCE`）」，并在脚注说明「若为原样下载则应为 L0，标 UNKNOWN 更保守」

**层级分布**：L0（`fd_*/`、`historical_160/`、`raw_snapshots/`、`lottery_results_raw.json`）｜ L1（主要采集表）｜ L2（派生）｜ L3（`results/` + 顶层研究文档）

### 第 3 项 · 修正数字自相矛盾 ✅ 已改

#### 3.1 复现受限资产数量

| | 原版 | 修订版 |
|---|---|---|
| 表述 | 「约 **30** 个（快照类 **82** 个 + 派生类）」 | **96 个** |
| 问题 | **82 + x ≠ 30，数字不自洽** | 已按四类互不重叠重统计 |

**重统计结果**：

| 类 | 成因 | 数量 |
|---|---|---|
| A | 时点快照 | **87** |
| B | 时间推进型账本 | **5** |
| C | 采集窗口已过 | **3** |
| D | 上游输入缺失 | **1** |
| | **合计** | **96** |

另单列 1 项「部分复现受限」（`e58_gap_backfill` 的 `poolStatus` 字段），**不计入合计**避免重复计数。

来源 `UNKNOWN` 的为 **14 条**（与上表部分重叠，已注明重叠范围）。

#### 3.2 Bug 数量 → 正式 Bug Registry

| | 原版 | 修订版 |
|---|---|---|
| 表述 | 「已知 bug **7** 个」 | **BUG-001 ~ BUG-009，共 9 个** |
| 问题 | 未区分发现时点、未给 ID、未标影响方向；且把 E53 的 3 处同源 bug 记为 1 个 | 已建立 Registry |

**划分**（锚点：结案日期 **2026-09-08**）：

- **结案前已知**：BUG-001 ~ BUG-006（**6 个**）
- **结案后新发现**：BUG-007 ~ BUG-009（**3 个**，均本轮 09-21 发现）

**状态**：FIXED 8 ／ PARTIALLY FIXED 1

**影响方向**：结果虚高 4 ／ 样本污染或遗漏 3 ／ 数据缺字段 1 ／ 账本停更 1

**⚠️ 未把历史改写成「当时已知」**：BUG-009 明确记录两阶段认知 —— 09-08 诊断（错误地归因于「脚本没挂常驻」）与 09-21 更正（真因为沙箱休眠杀进程），并注明**不追认为当时已知**。

### 第 4 项 · 降低无法完全证明的表述强度 ✅ 已改

**逐条替换（按 Reviewer 给的 3 个示例全部落实）**：

| 原句 | 修订后 |
|---|---|
| 「`bet_real.csv` 从未产生，因此真实下注记录从未产生」 | 「本次扫描的 `/workspace` 中**未发现** `data/bet_real.csv`，且当前识别到的代码链中**未发现**其他明确的真实下注账本；不能据此排除工作区之外存在相关记录」 |
| 「空目录说明该计划从未落地」 | 「**本次扫描时该目录为空**，现有工作区内未发现对应落盘资产」 |
| 「某网站 405，因此数据不可重建」 | 「通过**本次使用的访问路径暂时无法**重建，是否存在其他历史来源仍为 `UNKNOWN`」 |

**全局措辞降级**（不止上述 3 条）：

| 类别 | 处理 |
|---|---|
| 所有 `❌ 不可重建` / `❌ 窗口已过` / `❌ 时间推进` / `❌ 时点快照` / `❌ 输入不全` / `❌ 已停更` | **全部替换**为 `⚠️ …本次未发现重放路径（INFERENCE）` 等分级表述（共 12 处） |
| 所有跨源推断 | 标 **`INFERENCE`** |
| 所有无法证明项 | 标 **`UNKNOWN`** |
| §7.1 标题 | 「已知缺失文件」→「**本次扫描未见、但代码/清单中声明的文件**」 |
| §9 标题 | 「给『重新研究』的建议」→「**观察记录（非结论，不做建议）**」，并加列 `观察 / INFERENCE / UNKNOWN` 三栏 |
| §7.2 新增纪律 | 「四类均为 `INFERENCE`（机制推断），**非实测复现失败**；本次**未尝试**实际复现任何资产」 |

**主报告 §7 新增措辞纪律声明块**：观察到什么写什么 ／ 推断标 `INFERENCE` ／ 无法证明标 `UNKNOWN` ／ 不由单点观察推全局结论。

### 第 5 项 · 降级「外部存活测试」✅ 已改

- 主报告 §5 章标题保留（外部来源是任务第 4 项要求），但**拆为两节**：
  - **§5.1 / §5.2**：外部来源的**引用记录**（任务核心范围）
  - **§5.3**：数据源访问探测结果 → **标为 `Supplementary observation / 非任务核心证据`**
- 新增声明：「**该部分属于额外观察，不作为本次资产盘点的正式验收依据**」
- §5.3 表格新增「**不构成以下推论**」列，逐项写明该观察**不能**推出什么
- 新增措辞纪律：「不得由单次探测结果推出『永久不可用』『不可重建』等结论」
- §1.2 方法表中「数据源存活」改为「数据源访问探测 → Supplementary observation，非任务核心证据」
- **本次未访问任何新的外部站点**

### 第 6 项 · 补齐配套文件 ✅ 已核对

全部 **4 份**文件均**实际存在**（无 `NOT CREATED` 项）：

| 文件 | 状态 | 大小 |
|---|---|---|
| `INVENTORY-20260921-01_竞彩足球研究资产只读盘点.md` | 存在（已修订） | ~40KB |
| `清单_数据资产.md` | 存在（已修订） | ~72KB |
| `目录树.md` | 存在（已修订） | ~42KB |
| `资产关系说明.md` | 存在（已修订） | ~28KB |

> **原版问题澄清**：上一轮我曾用 `present_files` 交付全部 4 份，但 Reviewer 只看到主报告——**原因是交付渠道，不是文件缺失**。本版改为直接列出全部文件名与路径。

---

## 三·补 · 修订期间的新增观察（未影响数据，仅记录）

### 关于常驻进程的准确表述

**未主动停止任何进程**；修订期间观察到 5 个进程自行／因环境原因全部消失，**原因 `UNKNOWN`**。

| 观察项 | 值 |
|---|---|
| 11:0x（修订开始前） | 5 个进程均在（PID 99279 / 99327 / 99546 / 99673 / 99800） |
| 11:2x（修订期间） | 5 个进程均不在（`pgrep python3` 无输出） |
| 最后写盘 | `data/sporttery_odds.csv` 11:09:45 |
| 观察时点 | 11:24:59 |

**动作记录**：**本次未发出任何停止指令，也未重启**（Reviewer 明令「不停止常驻采集进程」，重启同属越界）。
⇒ 即「未主动停止」与「观察到进程消失」**两件事同时成立，不矛盾**：前者是本次的动作，后者是本次的观察。

**`INFERENCE`**：消失现象与主报告 BUG-009 记录的「沙箱环境杀进程」同类。
**`UNKNOWN`**：进程消失的确切时刻、机制与原因。

该观察已同步记入主报告 §0.1。

---

## 三·补2 · REV-2 修订的 4 处（Reviewer 第二轮指出）

> Reviewer 的判断**全部正确**，4 处均为我的错误。逐条处置如下。

### REV-2-1 · 常驻进程描述自相矛盾 ✅ 已改

**问题**：`REVISION_NOTES.md` 前半写「修订期间 5 个常驻进程全部停止」，后半禁止项核对表却写「不停止常驻采集进程 ✅（5 个进程全程运行）」。**两句不能同时成立。**

**处置**：按 Reviewer 给定写法更正为：

> **未主动停止任何进程**；修订期间观察到 5 个进程自行／因环境原因全部消失，**原因 `UNKNOWN`**。

并明确「未主动停止」（本次的**动作**）与「观察到进程消失」（本次的**观察**）是两件事，**不矛盾** —— 此点原版未说清，是矛盾根源。
禁止项核对表同步改为：`✅ 未主动停止任何进程。⚠️ 但修订期间观察到 5 个进程全部消失，原因 UNKNOWN`。

**另改正（Reviewer 同条指出）**：主报告原写「⇒ **采集链健康**」。
这是**把一个时点观察升级成了整体判定**。已改为：

> **扫描时点观察到** `sporttery_odds.csv`（10:24）、`forward_ledger.csv`（10:25）仍有正常写盘。
> ⚠️ 这是**一个时点的写盘观察**，**不构成「采集链健康」的整体判定**。

### REV-2-2 · `hafu_hist.csv` 的 Level 未按规则处理 ✅ 已改

**问题**：主报告脚注自己承认「Level 标 `UNKNOWN` 更保守」，但表中**仍写 `L2`** —— 违反刚确立的原则「无法判断就写 `UNKNOWN`，不要猜」。

**处置**：`hafu_hist.csv` 的 Level 由 `L2` **直接改为 `UNKNOWN`**（主报告 + `清单_数据资产.md` 两处同步）。
`hafu_hist_fitted.csv` 可确认为**拟合产物**（文件名含 `fitted`，与 `hafu_hist.csv` 行数/字段存在可辨差异），**维持 `L2`**。

**这是本次自查确认的错误**：写了原则却又保留旧值，属知行不一。

### REV-2-3 · 《目录树.md》旧版本残留 ✅ 已改

**问题**：头部声明「本文件不包含 `REPORTS/` 目录自身」，正文第一行却又有 `├── REPORTS/  [2 文件, 0.1MB]`，且其下含**已改名的中间产物** `_清单_数据资产.raw.md`、大小 35.9KB（旧版）。而修订包实际为 **5 份**。

**根因**：该条目是扫描器在 `REPORTS/` 创建**之前**识别到的旧状态，我在修订时改了头部声明却**没有清理正文**。

**处置**：采纳 Reviewer 给出的第二方案 —— **直接从树中删除 `REPORTS/` 那三行**（含 2 个子项）。
头部声明改为说明「本文件不含 `REPORTS/` 条目」及原因；并在文件末尾**人工补充**一节「附：当前 `REPORTS/` 实际内容（修订包，共 5 份）」，明确标注该节为人工补充、非扫描结果。

**未重新跑资产扫描。**

### REV-2-4 · `data/` 文件数口径不一致 ✅ 已改（查明根因）

**问题**：主报告写 `data/`：`195 + 子目录 376 ≈ 571`；`目录树.md` 写 `[575 文件]`。两数字未解释。

**实测复核结果**：

| 口径 | 数值 | 判定 |
|---|---|---|
| `data/` 直接文件（`maxdepth 1`） | 195 | 正确 |
| 子目录**递归**全量 | **380** | 正确 |
| **合计** | **195 + 380 = 575** | 与目录树一致 |
| 主报告原写「子目录 376」「≈571」 | 376 / 571 | ❌ **笔误，已作废** |

**根因**：我对子目录的计数出错（漏算/误算了深层文件），得到错误的 376。**目录树的 575 是正确的**。

**处置**：
- 主报告 §1.3 改为 `575（= 直接 195 + 子目录递归 380）`
- 新增**计数口径说明块**：明确「本报告所有文件数均指 `os.walk` 递归全量（含隐藏文件），与 `目录树.md` 一致」
- 主报告 §2.2 子目录表补「合计 380」行，并标注 `recommendations/`、`fixtures_snapshots/` 为 LIVE 目录（当前值已由 49→50、25→26 增长，**非口径差异**）
- 时点差异一并说明：扫描时点 575 → 当前复核 577（+2 来自 LIVE 目录，属写入非口径）

**未重新扫描全部资产**（仅对 `data/` 做了一次计数复核）。

---

## 四、哪些数字被重新统计

| 项 | 原值 | 修订值 | 说明 |
|---|---|---|---|
| 复现受限资产 | 「约 30（82+派生）」**自相矛盾** | **96** | 拆为 A87+B5+C3+D1 |
| Bug 数量 | 7 | **9** | 按「一 bug 一 ID」拆分 + 区分结案前后 |
| 来源不明文件类 | 6 类 | **8 类** | 原版漏计 `predictions/`、`raw_snapshots/` |
| 活跃（LIVE）文件 | 未统计 | **9** | 新增 |
| 层级分布 | 未统计 | L0/L1/L2/L3 | 新增（`Level` 列 374 行） |
| 扫描文件总数 | 863 | 863（不变） | 加注「时点值」 |
| CSV 文件数 | 374 | 374（不变） | |
| 重复文件组 | 48 组 / 138 文件 | 48 组 / 138 文件（不变） | |
| 前瞻账本 | H1 99 场 / H2 42 条 | 同（不变） | 复核哈希已变，但行数未变 |

---

## 五、哪些项目仍是 UNKNOWN

**是，仍有 UNKNOWN。** 分布如下：

### 5.1 来源/产出链未知（主报告 §7.3，8 类）

| # | 项目 | UNKNOWN 内容 |
|---|---|---|
| 1 | `data/hafu_hist.csv` / `hafu_hist_fitted.csv` | 上游输入；`hafu_hist_fitted` 的产出脚本 |
| 2 | `data/e16_three_season.json` | 产出脚本（工作区无 `e16_*.py`） |
| 3 | `data/betexplorer_odds.csv`、`be_results.json` | 产出链 |
| 4 | `data/historical_160/` 下 189 个文件 | 下载批次与来源 |
| 5 | `data/e23_result_2223-2324-2425.json` | 产出命令 |
| 6 | `data/.last_report`、`data/_heartbeat.log` | 产出方 |
| 7 | `data/predictions/` 内 5 个 CSV | 产出方 |
| 8 | `data/raw_snapshots/` 内 8 个 JSON | 产出方 |

### 5.2 资产关系未知（`资产关系说明.md` §5，14 条）

其中 9 条与上表重叠，另含：`e36_league_mismatch.json`、`jc_odds_clean*.csv`、`e30_crossplay.json` vs `e30b_control.json`、`e58_gap_backfill` 的完整复现命令、`team_name_map.json` 的完整可复现性。

### 5.3 新增的 UNKNOWN（本次修订引入）

| 项目 | UNKNOWN 内容 |
|---|---|
| `data/hafu_hist.csv` 的 **Level** | 字段结构与 football-data 原始 CSV 一致但未确认是否经转换；L0 或 L2 **未定** |
| 9 个 LIVE 文件的**当前值** | 扫描后已发生变化，**当前** SHA256/行数未逐一复核（仅复核了 9 个中的 2 个变化情况） |
| GitHub 阻断的**根因** | 观察到「本次未能连通」，但未排除其他可能原因（临时故障、凭据问题） |
| `data/e23_result.json` 重复的**覆盖方向** | 两文件 SHA256 相同，但谁覆盖谁未确认 |

### 5.4 仍未验证的事项

| 项目 | 说明 |
|---|---|
| 96 个复现受限资产的**实际复现可行性** | 判定为机制推断（`INFERENCE`），**本次未实际尝试复现任何资产** |
| `bet_real.csv` / `jc_history_okooo*.csv` 是否曾存在 | 仅知本次扫描未见 |
| 工作区外是否存在其他记录 | **未调查**（超出本任务范围） |

---

## 六、未做的事（按 Reviewer 明令）

| 禁止项 | 遵守 |
|---|---|
| 不重新跑任何历史实验 | ✅ |
| 不重新训练模型 | ✅ |
| 不修改旧数据 | ✅ |
| 不删除重复文件 | ✅（48 组重复**全部保留**） |
| 不重命名旧资产 | ✅ |
| 不整理目录结构 | ✅ |
| 不停止常驻采集进程 | ✅ **未主动停止任何进程**。⚠️ 但修订期间**观察到** 5 个进程自行／因环境原因全部消失（原因 `UNKNOWN`），详见 §三·补 |
| 不修代码 | ✅ |
| 不补历史数据 | ✅ |
| 不继续研究盈利结论 | ✅ |
| 不自行设计下一阶段研究方案 | ✅（原 §9「建议」已降级为「观察记录」） |
| 不处理 GitHub 上传 | ✅（§0.2 已降级为「非验收范围」） |
| 不访问新外部站点 | ✅ |

---

## 七、修订清单（文件级）

| 文件 | 修订内容 |
|---|---|
| `INVENTORY-20260921-01_竞彩足球研究资产只读盘点.md` | 新增 §0（在线盘点声明 + GitHub 降级）、§1.4（L0~L3 定义）、§2.4（LIVE 清单）；§2 加 Level/LIVE 列；§5 拆分并降级 §5.3；§7 全面重写（§7.1 措辞降级、§7.2 重统计、§7.3 补 2 类、§7.4 建 Bug Registry）；§8 汇总表重算；§9 降级为观察记录 |
| `清单_数据资产.md` | 新增 LIVE 说明表（9 文件）；9 行 SHA256 加 `LIVE@10:22:` 前缀；全表新增 `Level` 列（374 行）；头部加时点声明 |
| `目录树.md` | 头部加「在线盘点时点值」警示 + 2 文件已变实证；注明不含 `REPORTS/` 自身 |
| `资产关系说明.md` | 全局降级「❌ 不可重建」类表述（12 处）；数据流图加 L0~L3 标注；§4 溯源表加 `INFERENCE` 说明；§6 风险提示保留（本身为观察性表述） |
| `REVISION_NOTES.md` | 新建（本文件） |

## 八、REV-3 最终文档修订（GitHub 入库恢复）

本节记录 Reviewer 已明确验收过、但本机 OneDrive 留存副本尚未完整体现的 REV-3 修改。**未重新扫描、未重跑实验、未修改任何旧数据或旧研究代码。**

1. `forward_ledger.csv` 的 **99/250** 与 `clv_ledger.csv` 的 **42/100** 只保留为盘点时点值；删除「累计中／预计完成日期」，后续自动累计状态统一为 `UNKNOWN`。
2. BUG-009 不再把「沙箱休眠杀进程」写成已证实根因；仅保留「常驻进程消失／账本停更」的观察，根因标 `UNKNOWN`，现有编排与 gap backfill 仅视为缓解措施。
3. BUG-001～003 恢复为三个独立缺陷：
   - BUG-001：ROI 分母错误；
   - BUG-002：selection mask 漏乘 hit mask；
   - BUG-003：`cKDTree.query` 索引未正确展平。
4. 删除 Bug Registry 后残留的旧编号表两行（旧 `6`／`7`）。
5. §9 不再写「编排链当前处于活跃状态」，改为「扫描时点曾有写入活动；后续自动累计状态 `UNKNOWN`」。
6. 统计口径同步为「结果虚高／有利」**3 个：BUG-001、BUG-002、BUG-004**；正式入库时把残留句「4 个『虚高』类 bug」更正为「3 个」。

**来源说明**：这些修改来自 Reviewer 在同一盘点任务的最终复核结论；GitHub 入库过程只恢复既定 REV-3 文档状态，不新增研究结论。

---

**修订完成** ｜ 2026-09-21 11:2x ｜ 状态 `REVIEW`
